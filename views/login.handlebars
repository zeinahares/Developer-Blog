<section class="section">
    <div class="container">
        <div class="columns is-centered is-vcentered is-multiline">
            <div class="column is-full-desktop is-full-tablet">
                <h2 class="title is-4 has-text-centered">Login</h2>

                <form class="login-form">
                    <div class="field">
                        <label class="label" for="email-login">Email:</label>
                        <div class="control">
                            <input class="input" type="text" id="email-login" />
                        </div>
                    </div>
                    <div class="field">
                        <label class="label" for="password-login">Password:</label>
                        <div class="control">
                            <input class="input" type="password" id="password-login" />
                        </div>
                    </div>
                    <div class="field">
                        <div class="control has-text-centered">
                            <button class="button is-primary" type="submit">Login</button>
                        </div>
                    </div>
                </form>

                <h2 class="title is-4 has-text-centered pt-6">Don't have an account yet?
                    <a href="/signup"><span class="has-text-primary signup-link"> Sign up</span></a>
                </h2>
            </div>
        </div>
    </div>
</section>

<script src="./js/login-signup.js"></script>